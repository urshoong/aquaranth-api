<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dq.aquaranth.dept.mapper.DeptTreeMapper">

<!--    모달창에서 해당하는 회사의 하위 부서들을 조회합니다.-->
    <select id="getTreeModal" resultType="com.dq.aquaranth.dept.dto.DeptDTO">
        select * from tbl_dept where company_no = #{companyNo} order by ord;
    </select>










</mapper>
