<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dq.aquaranth.mygroup.mapper.MygroupMapper">

    <!--로그인한 사원의 마이그룹 생성-->
    <insert id="insert">
        insert into tbl_mygroup (emp_no, reg_user)
        values ((select emp_no
                 from tbl_emp
                 where username = #{username}),
                (select emp_name
                 from tbl_emp
                 where username = #{username}))
        ;
    </insert>


    <!--로그인한 사원의 마이그룹 수정-->
    <update id="update">
        update tbl_mygroup
        set mygroup_name = #{mygroupName}
          , mod_user = (select emp_name
                        from tbl_emp
                        where username = #{username})
          , mod_date = now()
        where emp_no = (select emp_no
                        from tbl_emp
                        where username = #{username})
        ;
    </update>


    <!--로그인한 사원의 마이그룹 삭제-->
    <delete id="delete">
        delete
        from tbl_mygroup
        where emp_no = (select emp_no
                        from tbl_emp
                        where username = #{username})
        ;
    </delete>
</mapper>
